enum UserType { client, lawyer, admin }

enum NavItem {
  home,
  calendar,
  mySchedule,
  booking,
  finance,
  chat,
  notifications
}

enum PopoverOption { none, myProfile, editProfile, changePassword }
